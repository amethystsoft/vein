name: Swift Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-22.04
    container:
      image: swift:6.2-noble

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests
        run: swift test
